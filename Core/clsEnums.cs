namespace Core
{
    public class clsEnums
    {
        public enum enMode { enAddNew, enEdit, enUpdate }
        public enum enMessageType { enSuccess, enWarning, enError, enInfo }
        public enum enStatus { Active, Inactive }
    }
}
